var express = require("express");
var router = express.Router();

router.get("/login", function() {});
router.post("/login_action", function() {});
router.get("/logout", function() {});

module.exports = router;